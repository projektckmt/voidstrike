name: htb-support
mode: ctf
targets:
  - 10.129.245.50
objective: "root flag"
expected_flags: 2  # user + root — gate forces analyst handoff once both are recorded
budget_usd: 5.0
profile: eco
vpn_config: ../../machines_us-3.ovpn  # path is resolved relative to this spec file
notes: |
  Retired HTB easy box. No HITL — run hot.